[pytest]
# 测试发现
testpaths = backend/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 异步配置
asyncio_mode = auto

# 输出格式
addopts =
    -v
    --tb=short
    --strict-markers
    -p no:warnings

# 标记
markers =
    slow: 标记慢速测试
    integration: 集成测试
    unit: 单元测试
